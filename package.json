{
  "name": "app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun frontend dev & bun backend dev",
    "build": "bun frontend build && bun backend build",
    "lint": "bun lint",
    "frontend": "bun run --filter app-frontend",
    "backend": "bun run --filter app-backend"
  },
  "devDependencies": {},
  "peerDependencies": {},
  "trustedDependencies": [
    "core-js",
    "protobufjs"
  ]
}